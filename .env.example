# Optional bridge authentication. Copy to .dev.vars for local Wrangler.
# BRIDGE_API_KEY=choose-a-separate-bridge-secret
# Custom destinations must be explicitly trusted by the deployment owner.
# ALLOWED_UPSTREAM_ORIGINS=https://gateway.example.com
# Browser clients need an explicitly allowed origin (no wildcard).
# CORS_ORIGINS=http://localhost:3000
